
import 'package:flutter/material.dart';

class whileLoding extends StatelessWidget {

  @override
  Widget build(BuildContext) {
    return Center(
      child: CircularProgressIndicator(
        color: Colors.amber
    
      ),
    );

    
  }
}